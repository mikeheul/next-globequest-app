/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "picfiles.alphacoders.com",
            "a.cdn-hotels.com",
            "cdn.bonsplansvoyage.fr"
        ]
    }
};

export default nextConfig;
